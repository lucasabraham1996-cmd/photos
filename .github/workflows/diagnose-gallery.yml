name: Diagnose gallery sources
on:
  push:
    paths:
      - .github/workflows/diagnose-gallery.yml
      - scripts/diagnose-gallery.py
  workflow_dispatch:
permissions:
  contents: read
jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Inspect published gallery tabs
        run: python3 scripts/export-public-gallery.py
